{{!< default}}

{{> menu}}

<div id="main-wrapper">
	{{! Header }}
	<header id="header">
		<button id="showLeft"><i class="icon-flickr"></i></button>
		<section data-height="420" class="heading-bg" {{#if @blog.cover}}style="background-image: url({{@blog.cover}})"{{/if}}>

			<section class="inner">
				<div class="head-content">
					<div class="centered">
						{{#if @blog.logo}}<a class="blog-logo" href="{{@blog.url}}"><img width="120" src="{{@blog.logo}}" alt="Blog Logo" /></a>{{/if}}
			  		<h1 class="blog-title"><a href="/">{{@blog.title}}</a></h1>
			  		<p class="blog-description">{{@blog.description}}</p>
					</div>
				</div>
		  </section>

		</section>
	</header>

	{{! {{> links}}

	<div class="container-wrap">

		<div class="container">
			<div class="row">
				<main id="main" class="main-content" role="main">
					{{! Our Loop }}
					{{#foreach posts}}

							  <div>
							    <a href="{{url}}" class="post-item">
							      <div class="caption">
							      	<h3>{{{title}}} - <span class="post-time">{{date published_at format="MMM DD"}}</span></h3>
							      </div>
							    </a>

							    {{excerpt words="50"}} ...
							    <a href="{{url}}"> Continue reading.</a>
								<br/>

						      </div>

						      <br/>

					{{/foreach}}
				</main>
			</div>
		</div>

		{{!! Pagination }}
		{{pagination}}